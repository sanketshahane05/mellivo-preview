const $=(s,c=document)=>c.querySelector(s);const $$=(s,c=document)=>[...c.querySelectorAll(s)];
window.addEventListener('pointermove',e=>{document.documentElement.style.setProperty('--mx',`${e.clientX}px`);document.documentElement.style.setProperty('--my',`${e.clientY}px`)},{passive:true});
const io=new IntersectionObserver(entries=>entries.forEach(x=>x.isIntersecting&&(x.target.classList.add('visible'),io.unobserve(x.target))),{threshold:.12});$$('.reveal').forEach(el=>io.observe(el));
const brandField=$('#brandField'),brandCard=$('#brandCard');if(brandField){brandField.addEventListener('pointermove',e=>{const r=brandField.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;brandCard.style.transform=`translate(-50%,-50%) rotateX(${-y*10}deg) rotateY(${x*13}deg) translate3d(${x*16}px,${y*12}px,18px)`;$$('.orbit',brandField).forEach((o,i)=>o.style.translate=`${x*(i+1)*4}px ${y*(i+1)*4}px`);$('.brand-halo',brandField).style.transform=`translate(${x*30}px,${y*30}px)`});brandField.addEventListener('pointerleave',()=>{brandCard.style.transform='translate(-50%,-50%) rotateX(0deg) rotateY(0deg)';$$('.orbit',brandField).forEach(o=>o.style.translate='0 0');$('.brand-halo',brandField).style.transform='translate(0,0)'})}
const stage=$('#decisionStage'),consoleCode=$('#traceConsole code');const traceCopy={1:'ingest authoritative context → normalize knowledge → preserve provenance_',2:'evaluate context → apply policy + evidence → resolve competing signals_',3:'produce governed outcome → attach rationale → expose review path_',4:'handoff approved decision → trigger workflow → retain audit trace_'};if(stage){$$('.decision-node',stage).forEach(node=>{node.addEventListener('pointerenter',()=>{stage.classList.add('active');$$('.decision-node',stage).forEach(n=>n.classList.remove('active'));node.classList.add('active');consoleCode.textContent=traceCopy[node.dataset.step]});node.addEventListener('pointerleave',()=>{stage.classList.remove('active');node.classList.remove('active');consoleCode.textContent='hover a stage to inspect the decision path_'})})}
const badger=$('#badgerStage');if(badger){badger.addEventListener('pointermove',e=>{const r=badger.getBoundingClientRect();const x=((e.clientX-r.left)/r.width)*100,y=((e.clientY-r.top)/r.height)*100;badger.style.setProperty('--bx',`${x}%`);badger.style.setProperty('--by',`${y}%`)})}
$$('.tilt-card').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(800px) rotateX(${-y*3.5}deg) rotateY(${x*4}deg) translateY(-3px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});
$$('.magnetic').forEach(el=>{el.addEventListener('pointermove',e=>{const r=el.getBoundingClientRect(),x=e.clientX-(r.left+r.width/2),y=e.clientY-(r.top+r.height/2);el.style.transform=`translate(${x*.08}px,${y*.08}px)`});el.addEventListener('pointerleave',()=>el.style.transform='')});
